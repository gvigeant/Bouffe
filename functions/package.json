{
  "name": "bouffe-functions",
  "version": "1.0.0",
  "description": "Firebase Functions pour Bouffe",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
